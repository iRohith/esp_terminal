/// Defines the static routes used within the application.
///
/// This class contains constant string values for each navigation route.
class Routes {
  /// The route for the home page.
  static const String home = '/';

  /// The route for the variables page.
  static const String variables = '/variables';

  /// The route for the keypad page.
  static const String keypad = '/keypad';

  /// The route for the charts page.
  static const String charts = '/charts';

  /// The route for the send file page.
  static const String sendFile = '/sendfile';

  /// The route for the logs page.
  static const String logs = '/logs';

  /// The route for the connection page.
  static const String connection = '/connection';

  /// The route for the settings page.
  static const String settings = '/settings';
}
